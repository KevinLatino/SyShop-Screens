// import axios from 'axios'
// import formatApiUrl from './format-api-url'

export const signOnWithGoogleAccount = async (userInformation, setSession) => {
  // const apiUrl = formatApiUrl(
  //   "/customers_service/sign_on_customer_with_google_account"
  // )

  // console.log(userInformation)

  // const { data, statusText } = await axios.post(apiUrl, {
  //   ...userInformation,
  //   phone_number: "88888888"
  // })
  // // const { data, statusText } = await axios.post(apiUrl, userInformation)
  // // temporal

  // if (statusText !== "OK") {
  //   throw Error("Could not sign in with a Google account")
  // }

  // const session = {
  //   token: data.token,
  //   customerId: data.user_id
  // }

  // setSession(session)
  console.log("Nouuu")
}
